# Shared Pass Contract

`pass.ts` is the canonical data contract for the frontend preview and backend pass creation flow.

Use `PassDesign` for editor/preview state, `CreatePassRequest` for the API payload, and `CreatePassResponse` for the generation result.
